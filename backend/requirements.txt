# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.10

# LLM provider SDKs (imported lazily - install the one you use)
anthropic>=1,<2
openai>=1.40,<3

# LangGraph and AI
# The court procedure's state machine (app/workflow/graph.py). It brings in
# langchain-core itself; the LLM layer talks to vendor SDKs directly, so no
# other LangChain packages are needed.
langgraph>=1.2,<2

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.12

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2

# Development
black==24.10.0
ruff==0.7.3
mypy==1.13.0
